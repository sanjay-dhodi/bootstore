import { userModel } from "../models/userModel.js";

export const signUp = (req, resp) => {
  try {
    
  } catch (error) {
    
  }
};

export const signIn = (req, resp) => {
  console.log("login");
};
